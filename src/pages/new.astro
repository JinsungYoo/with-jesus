---
export const prerender = false;
import { supabase } from '../lib/supabaseClient'

const siteUrl = import.meta.env.PUBLIC_SITE_URL ?? 'https://seorosaranghara.com';

return Response.redirect(`${siteUrl}/join`, 302)
---

<h1>✍️ Create a New Post</h1>
<form>
  <!-- 작성 폼 내용 -->
</form>
